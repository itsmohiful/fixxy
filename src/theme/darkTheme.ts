import { ThemeOptions } from "@mui/material";

const darkTheme: ThemeOptions = {
  palette: {
    mode: "dark",
    primary: {
      main: "#0288d1",
    },
    secondary: {
      main: "#7000ff",
    },
  },
};

export default darkTheme;
